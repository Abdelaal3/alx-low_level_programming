#include "main.h"

/**
 * _isdigit - checking for a digit (0 through 9)
 * @c: this is int to be checked
 * Return: 1 if c is a digit, 0 is not
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
