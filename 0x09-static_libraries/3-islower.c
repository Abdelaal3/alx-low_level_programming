#include "main.h"

/**
 * _islower - checking for lowercase character
 * @c: the character checking
 * Return: 1 if c is lowercase, 0 is not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
