#include "main.h"

/**
 * _abs - calculating the absolute value of an integer
 * @n: this is int to check
 * Return: absolut value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
